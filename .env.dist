# Zotero RAG Backend Configuration
# Copy this file to .env and fill in your values

# HuggingFace token for downloading models (required for some models like nomic-embed-text-v1.5)
# Get your token at: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# Model preset to use
# Options: cpu-only, gpu-high-memory, mac-mini-m4-16gb, remote-api
# Default: cpu-only (configure mac-mini-m4-16gb on Mac Mini M4)
MODEL_PRESET=cpu-only

# Optional: Custom paths for model weights and vector database
# MODEL_WEIGHTS_PATH=/path/to/models
# VECTOR_DB_PATH=/path/to/qdrant

# Optional: API keys for remote inference
# OPENAI_API_KEY=your_openai_key
# ANTHROPIC_API_KEY=your_anthropic_key
# COHERE_API_KEY=your_cohere_key

# Optional: Zotero local API URL (default: http://localhost:23119)
# ZOTERO_API_URL=http://localhost:23119

# Optional: Logging configuration
# LOG_LEVEL=INFO
# LOG_FILE=/path/to/app.log
